# Posts a morning habit and streak checklist to Discord, once, around 07:00 Europe/London.
# GitHub Actions cron drops many runs, so this fires every 30 min across 06:00-09:30 UTC (the 07:00-09:00
# UK window in BST and GMT) and the script posts once and claims (job, UK-day) so it never repeats.

name: Routine checklist

on:
  schedule:
    - cron: "9,39 6-9 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "Force a re-send: bypass the hour gate and the once-per-day claim"
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "22"
      - name: Healthcheck start
        uses: ./.github/actions/healthcheck
        with:
          ping-key: ${{ secrets.HEALTHCHECK_PING_KEY }}
          slug: routine
          status: start
      - run: node scripts/routine.mjs
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          DISCORD_WEBHOOK_ERRORS: ${{ secrets.DISCORD_WEBHOOK_ERRORS }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          DISCORD_WEBHOOK_ROUTINE: ${{ secrets.DISCORD_WEBHOOK_ROUTINE }}
          FORCE: ${{ inputs.force && '1' || '' }}
      - name: Healthcheck success
        uses: ./.github/actions/healthcheck
        with:
          ping-key: ${{ secrets.HEALTHCHECK_PING_KEY }}
          slug: routine
      - name: Healthcheck fail
        if: failure()
        uses: ./.github/actions/healthcheck
        with:
          ping-key: ${{ secrets.HEALTHCHECK_PING_KEY }}
          slug: routine
          status: fail
